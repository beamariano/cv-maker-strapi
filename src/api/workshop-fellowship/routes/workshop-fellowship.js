'use strict';

/**
 * workshop-fellowship router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::workshop-fellowship.workshop-fellowship');
