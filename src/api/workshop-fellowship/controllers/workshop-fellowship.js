'use strict';

/**
 * workshop-fellowship controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::workshop-fellowship.workshop-fellowship');
